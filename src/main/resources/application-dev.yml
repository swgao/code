spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/blog?useUnicode=true&characterEncoding=utf-8
    username: root
    password: ""
  jpa:
    hibernate:
      #每次连接数据库验证你的实体类是否和你数据库字段是否一致，会进行同步
      ddl-auto: update
    # sql 语句输入控制台
    show-sql: true